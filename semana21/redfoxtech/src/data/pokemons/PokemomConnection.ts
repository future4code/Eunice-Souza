import { BaseDatabase } from "../baseDatabase/connection"

export const getAllPokemons = async (rea: Request, res: Response) => {

    private static tableName = (input: any): Promise<Pokemom> => {
        return input.origin && input.destination
        ? new Name (input.id, input.name)
        : new Pokemom(input.id, input.pokemom)
    }

    private statica teblaName toPokemom

    const result = await BaseDatabase
                            .connectiion(Pokemons.tableName)

    return result.map(this.toPokemom)
}
